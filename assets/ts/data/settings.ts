import type {
    ISettings,
} from "../types/data";

export default {

    // The maximum number of players that Blood on the Clocktower supports.
    maxPlayers: 20,

    // The default number of players when the application first loads.
    startingPlayers: 10,

} as ISettings;
